# Jordan's job search workspace

`workspace/` contains the materials Jordan grants to Hatch for a Partner
Operations Manager application. The notes were created at different times and
have not been reconciled. The Agent should inspect the files rather than assume
the resume is authoritative.

`request.txt` is the ordinary request Jordan types into the Agent. It is demo
orchestration and is deliberately kept outside the granted workspace.
